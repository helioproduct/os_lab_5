#include <stdio.h>
#include <stdlib.h>

#include "declaration.h"

int main(void)      
{
    // float f = derivative(0, 0.01);
    // printf("%f", f);


    int n;
    int x, y;
    while (scanf("%d", &n) > 0)
    {
        if (n == 1)
        {
            
        }
        else if (n == 2)
        {
            
        }
    }
    return 0;
}

